# -K-Means-Clustering-on-Wine-Dataset
Problem Statement: The objective of this project is to group wines into distinct clusters based on their chemical properties using the K-Means clustering algorithm, without using predefined class labels.
